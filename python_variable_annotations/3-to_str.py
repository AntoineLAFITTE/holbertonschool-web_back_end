#!/usr/bin/env python3
"""Type-annotated function that returns the string representation of a float."""


def to_str(n: float) -> str:
    """
    Takes a float n and returns its string representation.

    Parameters:
    n: float the float value to be converted to a string.

    Returns:
    str: string representation of the float.
    """
    return str(n)
